# _archive — superseded skills

These skills are retained for reference but are **not active**. They were the
timaeus-research-flavoured Tide loop, which `../td-loop` explicitly replaces
("replaces the timaeus-flavoured tide / tide-pick / lean-formalisation trio").

- `tide/`, `tide-pick/` — Tide exploration loop. Tied to timaeus infra
  (`projects.json`, GPT-5.5 Pro credentials, the SRI portfolio) that does not
  apply to this personal setup. Superseded by `td-loop`.

`lean-formalisation` is **not** archived — it still carries the general
formalisation principles `td-loop` builds on, and remains active in `../`.
